{
  "name": "happicat-onchain",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "$MEOW token contract for happi cat — GIWA Sepolia (pure-JS toolchain, no native binaries)",
  "scripts": {
    "compile": "node scripts/compile.mjs",
    "deploy": "node scripts/deploy.mjs",
    "verify": "node scripts/verify.mjs",
    "test": "node scripts/integration-test.mjs",
    "demo:reward": "node scripts/demo-reward.mjs"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.4.2",
    "solc": "0.8.28",
    "viem": "^2.55.10"
  }
}
